# Generated by tools/derive.js. Not committed *yet* — nothing consumes them
# until the launcher exists, and the manifest's `generated` timestamp changes
# on every run, so committing now buys a diff on every invocation and nothing
# else. When the launcher ships these become part of the deliverable (a static
# page opened from file:// cannot fetch, so manifest.js has to be present) and
# this file should shrink accordingly.
manifest.json
manifest.js
